
import { splitString } from 'trailmap/helpers/split-string';
import { module, test } from 'qunit';

module('Unit | Helper | split string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = splitString([42]);
  assert.ok(result);
});

