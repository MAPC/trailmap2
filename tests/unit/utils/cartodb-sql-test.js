import cartodbSql from 'trailmap/utils/cartodb-sql';
import { module, test } from 'qunit';

module('Unit | Utility | cartodb sql');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartodbSql();
  assert.ok(result);
});
