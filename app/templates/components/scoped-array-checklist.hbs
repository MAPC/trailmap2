{{#each optionList as |option|}}
    {{ui-checkbox 
      class=(concat "item" " " "option" option.code)
      label=option.name 
      checked=(get this (concat "option" option.code))
      onChange=(action 'reset' (concat "option" option.code))}}
{{/each}}